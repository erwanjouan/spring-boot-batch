apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-batch-job.fullname" . }}-test-connection"
  labels:
    {{- include "spring-batch-job.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-batch-job.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
